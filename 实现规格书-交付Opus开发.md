# 书画室本地看板系统 · 实现规格书（交付 AI 开发）

> 你（开发者）需要实现一个运行在本地局域网的 Web 应用：把教务 App 导出的 Excel 变成可查询的看板，并支持老师上传学员作品与语音/文字评价。剩余课时可按"导入值"或"导入值−评价课次"两种口径显示。所有登录用户可查看和编辑一切数据，但每次编辑都要留痕并展示编辑状态角标。
> 本文档是唯一需求来源，按此实现，不要自行扩展功能。

---

## 1. 技术栈（指定，勿更换)

- 后端：**Python 3.11 + FastAPI + SQLite**（SQLAlchemy ORM），Excel 解析用 `pandas`（.xls 需 `xlrd`，.xlsx 用 `openpyxl`）。
- 前端：**Vue 3 + Vite**（或同等轻量方案），移动端优先的响应式布局（老师主要用手机）。
- 文件存储：本地磁盘 `./data/media/{学号}/{YYYY-MM}/`，数据库单文件 `./data/app.db`。
- 部署：单条命令启动（`python run.py` 或 docker-compose），监听 `0.0.0.0:8000`，局域网手机浏览器直接访问。
- **HTTPS 必须支持**：启动时自动生成自签名证书并同时监听 8443 端口（浏览器录音 API 要求安全上下文）。录音失败时前端自动降级为"上传音频文件"控件。

## 2. 账号与身份

- 简单账号系统：用户名 + 密码（bcrypt），字段：姓名、角色标签（校长/教务/老师，仅作显示用，**不做任何权限拦截**）、绑定班级（多选，仅用于老师首页默认筛选"我的班级"，不限制访问）。
- 所有登录用户可查看、编辑、导入、导出全部数据。未登录不可访问。
- 内置初始账号 admin/admin123，首次登录强制改密。用户管理页所有人可用（新增/停用账号）。

## 3. 数据模型

```
User(id, username, password_hash, name, role_label, class_bindings[], active)

Student(id, student_no 学号, name, gender, phone, phone_identity, alt_phone, alt_phone_identity,
        birthday, grade, school, address, source, follow_up_person, tags, remark,
        classes[], status, created_by, created_time, ...其余导入字段原样保留)

CourseAccount(id, student_id, course_name, course_type, class_name,
        purchased, gifted, consumed, refunded, remaining_imported, over_used,
        consumed_amount, remaining_amount, absent_count, expire_date, import_batch_id)

Order / Transaction —— 按导入表字段原样建表，只读展示，无需编辑功能

ImportBatch(id, file_type, filename, imported_by, imported_at, row_count, summary)

Artwork(id, student_id, class_name, course_name, lesson_date, photos[≤3],
        eval_type: voice|text|none, eval_text, eval_audio_path, rating(可空),
        created_by, created_at, edit_count, deleted)

FollowUp(id, student_id, alert_type, status: 待跟进|已跟进, edit_count, updated_by, updated_at)

EditLog(id, entity_type, entity_id, editor_id, edited_at, changes_json[{field, old, new}])

EvalTemplate(id, category 书法|美术|通用, text, sort)
```

## 4. Excel 导入（4 种格式，按列名自动识别）

导入方式：网页拖拽上传 → 预览前 5 行 → 确认 → **整表覆盖式更新**（同类型数据以本次导入为准；学员以 `学号` 为主键匹配，学号缺失时用 `姓名+手机号`）。每次导入记录 ImportBatch，全站顶栏显示"学员/课时数据截至：{最近一次报读课程导入时间}"。

### 4.1 在读学员名单（.xls）→ Student

列名（精确匹配）：`学员姓名, 性别, 手机号身份, 手机号, 微信绑定状态, 绑卡状态, 人脸采集状态, 备用手机号身份, 备用手机号, 来源, 出生日期, 所在班级, 年龄, 年级, 学号, 学校, 跟进人, 学管师, 住址, 标签, 备注, 学员创建人, 创建时间`

### 4.2 学生报读课程（.xls）→ CourseAccount（核心表）

列名：`学员姓名, 手机号身份, 手机号, 所在班级, 课程名称, 课程类型, 购买数量, 赠送数量, 消耗数量, 退转数量, 剩余数量, 超上数量, 课消金额, 剩余课消金额, 缺课次数, 跟进人, 学管师, 到期时间, 性别, ...(其余同学员表), 停课时间, 复课时间, 停课备注`

注意：课时数量列的值形如 `"15课时"`，解析为整数 15；金额为数值；到期时间格式 `2026/08/28`。此文件可能含名为 `hidden` 的多余 sheet，只读第一个 sheet `报读课程`。**导入本表时同步建立"所在班级 → 课程名称"映射表**（供作品评价关联课程用）。

### 4.3 订单导出（.xlsx）→ Order（只读展示）

列名：`订单号, 流水号（支付/退款）, 学生姓名, 手机号, 订单类型, 购买项目, 应收/应退, 实收/实退, 欠费金额, 订单来源, 业绩归属人, 经办人, 创建时间, 经办时间, 上次推送时间, 最近支付时间, 先学后付订单状态, 订单状态, 备注, 留言, 学生创建人`

### 4.4 收支明细（.xls）→ Transaction（只读展示）

列名：`创建时间, 收支项目, 收支类型, 状态, 收支金额, 支付方式, 收支账户, 经办日期, 经办人, 关联订单号, 收付款人, 支付流水号, 交易失败原因, 备注, 校区名称`

### 4.5 导入报告

导入完成显示：新增/更新学员数、课程账户数、无法匹配学员的行（列出行号与原因）。导入报读课程表后额外生成 **偏差报告**（见 6.3）。

## 5. 页面与功能

### 5.1 学员看板
- 列表：搜索（姓名/学号/手机号模糊）、筛选（班级、课程、跟进人）、每行显示剩余课时角标。
- 详情页：基本信息卡（可编辑）＋ 各课程课时账户卡（含两种口径余额）＋ 作品时间轴（倒序，图片瀑布 + 语音播放器 + 文字）。
- 时间轴支持"导出该生作品集"：生成含照片、评价文字、日期的单页 HTML（打印即 PDF），语音评价以文件链接附带。

### 5.2 老师工作台（手机端主页）
- "我的班级"卡片（按账号绑定预筛，可切换看全部）→ 班级学员九宫格（姓名+剩余课时角标）→ 点学员进入上传页。
- 上传页：拍照/选图 ≤3 张 → 评价方式三选一（按住录音｜文字输入+模板快捷选｜跳过）→ 可选评级（优/良/需加强）→ 提交。上课日期默认今天，可改。
- 评语模板库：单独管理页，所有人可增删改，按 书法/美术/通用 分组。

### 5.3 课时提醒看板
- 三个 Tab：续费预警（估算剩余 ≤ 阈值，默认 3）、到期预警（到期 ≤ 14 天）、缺课关注（缺课次数 ≥ 2）。阈值在设置页可改。
- 每条可标记"已跟进"（FollowUp 记录，编辑留痕）。

### 5.4 订单 / 收支页
- 纯只读表格 + 时间范围筛选 + 合计行。无编辑。

### 5.5 设置页
- 余额口径开关（导入口径 / 估算口径，默认估算）、提醒阈值、备份状态（上次备份时间 + 手动备份按钮）、用户管理。

## 6. 剩余课时两种口径

### 6.1 导入口径
直接显示 `CourseAccount.remaining_imported`。

### 6.2 估算口径（默认）
```
估算剩余 = remaining_imported − 该学员该课程在 import_batch.imported_at 之后的评价课次数
评价课次数 = COUNT(DISTINCT lesson_date)，条件：Artwork.student_id 匹配、
            Artwork 所属班级映射到的课程 = 该课时账户的课程、deleted = false
```
- 同一学员同班级同 lesson_date 的多条 Artwork 记 1 课次；删除该日全部记录则回退。
- 界面显示 `约 N 课时`，点击展开："导入时 7（8月1日）− 评价 2 次 = 约 5"。
- 提醒看板的阈值判断使用当前口径下的值。

### 6.3 偏差报告
每次导入"学生报读课程"后，对上一批次生成对比：`上期估算剩余 vs 本期导入剩余`，差值 ≠ 0 的学员列成一张表（学员、课程、估算、实际、差值），存入 ImportBatch.summary 供随时查看。不做任何自动修正。

## 7. 编辑留痕（全局机制）

适用于一切可编辑对象：学员信息、作品与评价、跟进状态、评语模板。

- 任何编辑保存时：`edit_count += 1`，写入一条 EditLog（编辑人、时间、逐字段 old→new；图片/音频变更记录文件名增删）。
- **角标规则**：`edit_count = 0` 不显示；`= 1` 显示"已编辑"；`= 2` 显示"已二次编辑"；`≥ 3` 显示"已多次编辑"。角标出现在被编辑内容的卡片右上角，样式醒目（如橙色小标签）。
- 点击角标弹出编辑记录：按时间倒序列出每次"谁、何时、改了什么字段、改前→改后"。
- 删除作品为软删除（deleted 标记），同样写 EditLog，时间轴中不显示但编辑记录可查。
- 导入覆盖**不算编辑**，不触发角标（只更新 ImportBatch）。

## 8. 备份

- 每日 02:00 自动把 `app.db` + `media/` 增量打包到 `./backups/YYYY-MM-DD/`；保留最近 30 份。
- 设置页显示上次备份时间；超过 48 小时全站顶部黄条提示。手动备份按钮立即执行。

## 9. 验收标准（逐条自测）

1. 上传四个真实导出文件均能正确解析入库；"15课时"解析为 15；重复导入同一文件数据不重复。
2. 手机（局域网 HTTPS）可完成：登录 → 进班级 → 选学员 → 拍照 → 按住录音 → 提交，全程 ≤ 60 秒；HTTP 环境下录音控件自动替换为文件上传。
3. 给某学员连续两天各传一次评价后，估算余额比导入值少 2；同一天传两条只少 1；删除当天全部评价后余额回退。
4. 修改一条评价文字 → 卡片出现"已编辑"；再改 → "已二次编辑"；第三次 → "已多次编辑"；点击可见三条完整 old→new 记录。
5. 提醒看板三个 Tab 数字与手工按规则统计一致；切换口径后数字随之变化。
6. 导出某学员作品集 HTML，在手机浏览器可正常查看并打印。
7. 拔掉服务器网线（保持局域网），系统一切功能正常（验证零公网依赖）。

## 10. 明确不做

排课、点名、销课写回、在线支付、消息推送、家长端、多校区、权限拦截、报表图表。
